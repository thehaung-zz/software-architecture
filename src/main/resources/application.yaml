spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: p@ssw0rd1
    url: jdbc:mysql://localhost:3306/employeeDB
    username: root
  jpa:
    hibernate:
      ddl-auto: update
welcome:
  message: 'Welcome to Development Environment'